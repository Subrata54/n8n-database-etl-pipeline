# n8n-database-etl-pipeline
I built an automated data pipeline that takes new data from a company's database, cleans and validates it, loads it into their reporting warehouse, and notifies the team automatically—eliminating manual work and reducing errors
